<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.informatics.research.quiznav">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.quizes.QuizesMainListActivity"
            android:label="@string/title_activity_quizes_main_list"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".activities.quizes.QuizActivity" />
        <activity
            android:name=".activities.quizes.QuizesListActivity"
            android:label=""
            android:theme="@style/AppTheme.Materi" />
        <activity
            android:name=".activities.home.HomeActivity"
            android:label="Quiz Navigation" />
        <activity android:name=".activities.materialList.MaterialListActivity" />
        <activity
            android:name=".activities.login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>